<<<<<<< HEAD
#include <stdio.h>
#include <stdlib.h>
int vtCodigo[100];
float vtValor[100];
int vtEstoque[100];
char vtProduto[100][20];
int vtCodigoVenda[100];
int vtQtdVenda[100];
int qtdItens;

void fExibe(){
int posicao;
printf("********Funcao exibe ***************\n");
for (posicao=0;posicao<qtdItens;posicao++){
      printf("O codigo do produto da posicao  :%i", posicao);
      printf(" eh:  %i\n", vtCodigo[posicao]);
      printf("O valor do produto da posicao  :%i", posicao);
      printf(" eh: %.2f \n", vtValor[posicao]);
      printf("A qtd de estoque do produto da posicao : %i", posicao);
      printf(" eh: %i \n", vtEstoque[posicao]);
      printf("O nome do produto da posicao  : %i", posicao);
      printf(" %10s\n", vtProduto[posicao]);
    }     
  }



void fVenda(int codigoBusca, int posicao){
   int qtdCompra;
   printf("**********Funcao venda *********\n");
   printf("Informe uma quantidade de compra");
   scanf("%i", &qtdCompra);
   if (qtdCompra > vtEstoque[posicao]){
              printf("Estoque menor que o valor informado");        
            }
   else{
         printf("O valor comprado foi: %i", qtdCompra);
         vtCodigoVenda[posicao]=vtCodigo[posicao];
         vtQtdVenda[posicao]=qtdCompra;
         vtEstoque[posicao]=vtEstoque[posicao]-qtdCompra;   
      }           
      
      
  }

void fBuscaCodigo(){
int posicao;
int codigoBusca;
printf("*************** Funcao Busca *********\n");
printf("Informe um codigo de produto: " );
scanf("%i", &codigoBusca);
for (posicao=0;posicao<qtdItens;posicao++){
   if (codigoBusca == vtCodigo[posicao]){
      printf("O codigo do produto da posicao  :%i", posicao);
      printf(" eh:  %i\n", vtCodigo[posicao]);
      printf("O valor do produto da posicao  :%i", posicao);
      printf(" eh: %.2f \n", vtValor[posicao]);
      printf("A qtd de estoque do produto da posicao : %i", posicao);
      printf(" eh: %i \n", vtEstoque[posicao]);
      printf("O nome do produto da posicao  : %i", posicao);
      printf(" %10s\n", vtProduto[posicao]);
      system("cls");
      fVenda(codigoBusca, posicao);
      fExibe();
    }
}
     
 }


void fInsere(){
int posicao;
printf("**********Funcao insere ***********\n");
for (posicao=0;posicao<qtdItens;posicao++){
      printf("Informe o codigo do produto da posicao  :%i", posicao);
      scanf("%i", &vtCodigo[posicao]);
      printf("Informe o valor do produto da posicao  :%i", posicao);
      scanf("%f", &vtValor[posicao]);
      printf("Informe a qtd de estoque do produto da posicao : %i", posicao);
      scanf("%i", &vtEstoque[posicao]);
      printf("Informe o nome do produto da posicao  : %i", posicao);
      scanf("%s", vtProduto[posicao]);

    }     
  }

int main()
{
  printf("Informe a quantidade de itens");
  scanf("%i", &qtdItens);
  fInsere();
  fExibe();
  fBuscaCodigo();
  //exibir total de vendas
  system("pause");   
  return 0;
}
=======
#include <stdio.h>
#include <stdlib.h>
int vtCodigo[100];
float vtValor[100];
int vtEstoque[100];
char vtProduto[100][20];
int vtCodigoVenda[100];
int vtQtdVenda[100];
int qtdItens;

void fExibe(){
int posicao;
printf("********Funcao exibe ***************\n");
for (posicao=0;posicao<qtdItens;posicao++){
      printf("O codigo do produto da posicao  :%i", posicao);
      printf(" eh:  %i\n", vtCodigo[posicao]);
      printf("O valor do produto da posicao  :%i", posicao);
      printf(" eh: %.2f \n", vtValor[posicao]);
      printf("A qtd de estoque do produto da posicao : %i", posicao);
      printf(" eh: %i \n", vtEstoque[posicao]);
      printf("O nome do produto da posicao  : %i", posicao);
      printf(" %10s\n", vtProduto[posicao]);
    }     
  }



void fVenda(int codigoBusca, int posicao){
   int qtdCompra;
   printf("**********Funcao venda *********\n");
   printf("Informe uma quantidade de compra");
   scanf("%i", &qtdCompra);
   if (qtdCompra > vtEstoque[posicao]){
              printf("Estoque menor que o valor informado");        
            }
   else{
         printf("O valor comprado foi: %i", qtdCompra);
         vtCodigoVenda[posicao]=vtCodigo[posicao];
         vtQtdVenda[posicao]=qtdCompra;
         vtEstoque[posicao]=vtEstoque[posicao]-qtdCompra;   
      }           
      
      
  }

void fBuscaCodigo(){
int posicao;
int codigoBusca;
printf("*************** Funcao Busca *********\n");
printf("Informe um codigo de produto: " );
scanf("%i", &codigoBusca);
for (posicao=0;posicao<qtdItens;posicao++){
   if (codigoBusca == vtCodigo[posicao]){
      printf("O codigo do produto da posicao  :%i", posicao);
      printf(" eh:  %i\n", vtCodigo[posicao]);
      printf("O valor do produto da posicao  :%i", posicao);
      printf(" eh: %.2f \n", vtValor[posicao]);
      printf("A qtd de estoque do produto da posicao : %i", posicao);
      printf(" eh: %i \n", vtEstoque[posicao]);
      printf("O nome do produto da posicao  : %i", posicao);
      printf(" %10s\n", vtProduto[posicao]);
      system("cls");
      fVenda(codigoBusca, posicao);
      fExibe();
    }
}
     
 }


void fInsere(){
int posicao;
printf("**********Funcao insere ***********\n");
for (posicao=0;posicao<qtdItens;posicao++){
      printf("Informe o codigo do produto da posicao  :%i", posicao);
      scanf("%i", &vtCodigo[posicao]);
      printf("Informe o valor do produto da posicao  :%i", posicao);
      scanf("%f", &vtValor[posicao]);
      printf("Informe a qtd de estoque do produto da posicao : %i", posicao);
      scanf("%i", &vtEstoque[posicao]);
      printf("Informe o nome do produto da posicao  : %i", posicao);
      scanf("%s", vtProduto[posicao]);

    }     
  }

int main()
{
  printf("Informe a quantidade de itens");
  scanf("%i", &qtdItens);
  fInsere();
  fExibe();
  fBuscaCodigo();
  //exibir total de vendas
  system("pause");   
  return 0;
}
>>>>>>> 27c3d81ddee42f18cb72a0dfeac4f194ac9709b5
